import { CommonProfilePage } from '@/common/profile/CommonProfilePage';

/** Hồ sơ khách hàng (cổng account) — dùng chung `CommonProfilePage`. */
export default function ProfilePage() {
  return <CommonProfilePage />;
}
